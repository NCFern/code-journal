// /* global data */

// take image from input-photo (url) and show it on photo-preview //

// make querySelector to target the input-photo image //
// addEventListener to listen for inputs of //

// make 2 query selectors to take photo and show it//

// var imageUrlInput = document.querySelector('.url');

// imageUrlInput.addEventListener('input', showImage);

// function showImage(event) {
//   if (imageUrlInput.value !== '') {
//     imageUrls.push(imageUrlInput.value);
//   }
//   imageUrlInput.value = '';

// }
